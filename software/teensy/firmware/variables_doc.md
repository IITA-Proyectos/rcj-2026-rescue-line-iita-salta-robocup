# Deprecated TDP Copy

This file used to contain a duplicated draft of the Technical Description Paper.
It should not be used as a firmware variable reference or as a TDP source,
because duplicated documentation becomes obsolete quickly.

Canonical TDP source:

- `../../../TDP.md`

Firmware evidence for the TDP should be read from:

- `src/main.cpp`
- `src/priority_fix_flags.h`
- `platformio.ini`
- `lib/drivebase/`
- `lib/claw/`
- `test/`
